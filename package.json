{
  "name": "toggl-rofi",
  "version": "1.0.0",
  "description": "A script that interacts between rofi and the toggl-api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kirens",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.1.0",
    "toggl-api": "^1.0.1"
  }
}
